---
- name: Converge
  hosts: all
  become: true
  vars:
    minerva_user: minerva
  pre_tasks:
    - name: Create minerva user
      ansible.builtin.user:
        name: "{{ minerva_user }}"
  roles:
    - role: harden