---
- name: Create minerva application user
  ansible.builtin.include_tasks: user-setup.yml
  vars:
    target_user: "{{ bootstrap_minerva_user }}"
    target_group: "{{ bootstrap_minerva_group }}"
    target_uid: "{{ minerva_puid | default(omit) }}"
    target_gid: "{{ minerva_pgid | default(omit) }}"

- name: Create ansible service account
  ansible.builtin.include_tasks: user-setup.yml
  vars:
    target_user: "{{ bootstrap_deploy_user }}"
    target_group: "{{ bootstrap_deploy_user }}"

- name: Configure passwordless sudo for ansible service account
  ansible.builtin.include_tasks: pwless-sudo.yml
  vars:
    target_user: "{{ bootstrap_deploy_user }}"
