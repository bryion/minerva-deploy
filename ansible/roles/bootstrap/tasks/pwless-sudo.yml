---
- name: Configure passwordless sudo for {{ target_user }}
  ansible.builtin.copy:
    content: "{{ target_user }} ALL=(ALL) NOPASSWD: ALL\n"
    dest: "/etc/sudoers.d/10-{{ target_user }}"
    owner: root
    group: root
    mode: "0440"
    validate: /usr/sbin/visudo -cf %s