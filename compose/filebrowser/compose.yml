services:
  filebrowser:
    image: filebrowser/filebrowser:v2.32.0
    container_name: filebrowser
    user: 1000:1000
    ports:
      - 127.0.0.1:8383:80
    volumes:
      - './filebrowser_config:/config'
      - './filebrowser_database:/database'
      - '/opt/minerva-deploy:/srv'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
