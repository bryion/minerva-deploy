services:  
  uptime-kuma:
    image: louislam/uptime-kuma:1.23.16
    container_name: uptime-kuma
    volumes:
      - ./data:/app/data
    ports:
      - 127.0.0.1:3001:3001
    restart: unless-stopped