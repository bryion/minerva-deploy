---
# For configuration options and examples, please see:
# https://gethomepage.dev/en/configs/widgets

- datetime:
    format:
        timeStyle: short
        hour12: true

- logo: 
    icon: /images/logo-n-128.png

- glances:
    url: http://192.168.1.19:61208/
    version: 4 # required only if running glances v4 or higher, defaults to 3
    cpu: true # optional, enabled by default, disable by setting to false
    mem: true # optional, enabled by default, disable by setting to false
    cputemp: true # disabled by default
    uptime: true # disabled by default
    expanded: false # show the expanded view    
    diskUnits: bytes # optional, bytes (default) or bbytes. Only applies to disk
    disk: 
        - /
        - /media/drive2 # disabled by default, use mount point of disk(s) in glances. Can also be a list (see below)
#    label: MyMachine # optional
#    username: user # optional if auth enabled in Glances
#    password: pass # optional if auth enabled in Glances
#    unit: imperial # optional for temp, default is metric


#- resources:   
    # The cpu and memory resource information are the container's usage while 
    # glances displays statistics for the host machine on which it is installed.
#    cpu: true
#    cputemp: true
#    memory: true
#    uptime: true
#    units: metric
#    disk:
#        - /
#        - /media/drive2

#- greeting:
#    text_size: xl
#    text: " Minerva  "

#- openmeteo:
#    label: # optional
#    latitude: 37.3
#    longitude: -122.03
#    timezone: America/Los_Angeles # optional
#    units: imperial
#    cache: 5 # Time in minutes to cache API responses, to stay within limits